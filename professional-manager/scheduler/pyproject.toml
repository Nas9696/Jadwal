[project]
name = "professional-manager-scheduler"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.11,<3", "ortools>=9.14,<10"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pm_scheduler"]
