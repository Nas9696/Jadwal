# Contracts

Versioned API contracts live at the `/api/v1` boundary. TypeScript mirrors are kept explicit until automated OpenAPI generation is introduced; CI type-checks consumers to prevent silent drift.

